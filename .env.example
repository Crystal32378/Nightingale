# MiniMax Audio — put the real values in .env (gitignored), never here.
# Get both from the MiniMax platform console: https://platform.minimax.io
MINIMAX_API_KEY=
# Not needed by the t2a_v2 endpoint, kept for reference / older endpoints.
MINIMAX_GROUP_ID=
